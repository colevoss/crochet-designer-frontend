export type Maybe<T> = T | undefined;

export type CellDimensions = [x: number, y: number, w: number, h: number];
export type Coord = [col: number, row: number];
export type Point = [x: number, y: number];
